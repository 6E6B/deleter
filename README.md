# Deleter

A small Go desktop app for deleting Discord messages from a selected DM or channel.

## Requirements

- Go 1.26.2 or newer

## Run

```sh
go run .
```

## Project Layout

- `main.go` starts the app.
- `ui/` contains the desktop UI.
- `discord/` contains Discord API and session code.
